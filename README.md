# CogenticLab Connect Tool Library Skill
Interact with remote tool libraries via HTTP API. Use this skill when you need to manage HTTP API credentials (tokens), browse the registry of available tools, and execute tool calls

- Securely access **1,000+ tools in-house** with a single CLI
- **98% token** savings on tool invocations
- **No MCP client** needed to call MCP tools
- Turn **OpenAPI servers** into tools instantly
- Works with **any agent skill**

# AI Agent can access all user-registered tools
[Cogentic Hub](https://github.com/cogenticlab/cogentichub/)

- Establish a Tool Library to allocate tools on demand
- Different Tool Library keys allow access to different tool sets
- Direct access to MCP tools without requiring an MCP Client
- Supports invocation via HTTP API

# Supports progressive tool disclosure
Tool Library → Tool Category → Tool

- Custom tool categorization for greater token efficiency
- Enables dynamic skills with on-demand tool updates
- Identifies target tools from a pool of tens of thousands with an average of just 1,000 tokens

# Team collaboration:
Share tools within teams

# Deploy anywhere, access anywhere
Users can deploy private tools in any environment, and AI Agents can access these tools from any environment